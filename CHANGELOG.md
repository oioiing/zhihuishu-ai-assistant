# Changelog

## [Unreleased]

### Added

- 初始化 `CHANGELOG.md` 与 `.gitignore`。

### Changed

- 项目目录重构为 `src/`、`assets/`、`docs/` 分层结构。
- 更新 `manifest.json` 路径配置以匹配新结构。
- 更新文档链接路径，确保 `README` 与 `docs` 内互链可用。

### Fixed

- 修复文档中因目录迁移导致的相对路径失效问题。
